import 'package:flutter/material.dart';

class Expenses_List extends StatelessWidget {
  const Expenses_List({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}